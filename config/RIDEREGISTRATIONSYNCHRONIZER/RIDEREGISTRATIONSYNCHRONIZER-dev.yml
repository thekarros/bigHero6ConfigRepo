eureka:
  client:
    serviceUrl:
      defaultZone: ${eureka.instances}

kafka:
  consumer-group: rideRegistrationChange-kafka-consumer-group-use2dev
  topic:
    ride-registration-change: rideRegistrationChange-p01use2dev
    msk-suffix:
      ride-registration-change: rideRegistrationChange-use2dev